package util.values

enum class ToggleValue(val value: Boolean) {
    ON(true),
    OFF(false)
}

enum class ToggleName(val value: Int) {
    PERFORMANCE_COOKIES(1)
}

enum class FunctionalCookieIos(val value: Int) {
    FUNCTIONAL_COOKIE(2)
}
